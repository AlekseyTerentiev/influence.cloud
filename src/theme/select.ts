import { Theme } from '@material-ui/core';

export const MuiSelect = (theme: Theme) => ({
  root: {
    paddingRight: 21,
  },
  icon: {
    color: '#a5a8ab',
    fontSize: '1.4rem',
    top: 'calc(50% - 11px)',
  },
});
