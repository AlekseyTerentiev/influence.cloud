import { Theme } from '@material-ui/core';

export const MuiSelect = (theme: Theme) => ({
  root: {
    paddingRight: 22,
  },
  icon: {
    color: '#8e9194',
    fontSize: '1.4rem',
    top: 'calc(50% - 10px)',
  },
  iconOutlined: {
    right: 14,
  },
});
